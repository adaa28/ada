#include <stdio.h>

int main() {
    int marks[] = {50, 65, 78, 90};
    int n = sizeof(marks) / sizeof(marks[0]);

    printf("Updated Marks: ");
    for (int i = 0; i < n; i++) {
        marks[i] += 5;
        printf("%d ", marks[i]);
    }

    return 0;
}
