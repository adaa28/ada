#include <stdio.h>

int main() {
    int arr[] = {2, 10, 10, 100, 2, 10, 11, 2, 11, 2};
    int n = sizeof(arr) / sizeof(arr[0]);
    int visited[1000] = {0};

    printf("Duplicates: ");
    for (int i = 0; i < n; i++) {
        if (visited[arr[i]] == 1) {
            printf("%d ", arr[i]);
        }
        visited[arr[i]]++;
    }

    return 0;
}
