#include <stdio.h>

int main() {
    int marks[] = {50, 65, 78, 90};
    int n = sizeof(marks) / sizeof(marks[0]);
    int sum = 0;

    for (int i = 0; i < n; i++) {
        sum += marks[i];
    }
    printf("Average score: %.2f\n", (float)sum / n);

    return 0;
}
